<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendance_system">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".addnewfaculty"></activity>
        <activity android:name=".students_Display" />
        <activity android:name=".admin_register" />
        <activity android:name=".faculty_page" />
        <activity android:name=".update_delete" />
        <activity android:name=".dispayfacluties" />
        <activity android:name=".addnewstudent" />
        <activity android:name=".admin_page" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>